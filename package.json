{
  "name": "eon_app",
  "version": "1.0.0",
  "description": "EON Application",
  "scripts": {
    "install": "cd backend && npm install && cd ../frontend && npm install",
    "build": "cd backend && npm run build && cd ../frontend && npm run build",
    "start": "cd backend && npm start"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "pg": "^8.13.3"
  }
}
